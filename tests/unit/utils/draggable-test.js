import draggable from 'dummy/utils/draggable';
import { module, test } from 'qunit';

module('Unit | Utility | draggable', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = draggable();
    assert.ok(result);
  });
});
