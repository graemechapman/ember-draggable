{{yield (hash
  list=(component 'draggable-list' isGrouped=true allowHorizontal=allowHorizontal mouseX=mouseX mouseY=mouseY events=events activeIndex=activeIndex activeGroup=activeGroup dropGroup=dropGroup dropTarget=dropTarget activeItem=activeItem)
)}}